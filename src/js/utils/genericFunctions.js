export var compareArrays = (arrayOne, arrayTwo) =>{
  console.log(arrayOne.length);
  console.log(arrayTwo);
  if(arrayOne.length != arrayTwo.length){
    return false;
  }

  // arrayOne.forEach((data)=>{
  //   console.log(data);
  // })

}
